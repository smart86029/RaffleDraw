<UserControl x:Class="RaffleDraw.Wpf.Views.ResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:RaffleDraw.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding Source={StaticResource Locator}, Path=ResultViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" MinWidth="200">
            <TextBlock Text="抽獎" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Label Content="獎項" Width="100" VerticalAlignment="Center" />
            <ComboBox MinWidth="300" Margin="5" ItemsSource="{Binding Prizes}" SelectedItem="{Binding Prize}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding SerialNumber, StringFormat=\{0\,10\}}" Width="Auto" FontFamily="Consolas" />
                            <TextBlock Text=" | " Margin="10,0" />
                            <TextBlock Text="{Binding Content}" Width="Auto" />
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Label Content="序號" Width="100" VerticalAlignment="Center" />
            <TextBox HorizontalAlignment="Left" VerticalAlignment="Top" AcceptsReturn="False" Width="100" Margin="5" Text="{Binding SearchSerialNumber, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SearchEmployeeCommand}" />
                </TextBox.InputBindings>
            </TextBox>
            <Button Content="搜尋" HorizontalAlignment="Left" VerticalAlignment="Center" Command="{Binding SearchEmployeeCommand}" />
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" >
            <Label FontSize="24" Content="獎項" VerticalAlignment="Center" Margin="0,0,20,0" />
            <Label FontSize="32" Width="150" Content="{Binding Prize.SerialNumber}" />
            <Label FontSize="24" Content="數量" VerticalAlignment="Center" Margin="0,0,20,0" />
            <Label FontSize="32" Width="50" Content="{Binding Prize.Quentity}" />
            <Label FontSize="24" Content="已抽" VerticalAlignment="Center" Margin="0,0,20,0" />
            <Label FontSize="32" Width="50" Content="{Binding Prize.Winners.Count}" />
            <Label FontSize="24" Content="提供者" VerticalAlignment="Center" Margin="0,0,20,0" />
            <Label FontSize="32" Content="{Binding Prize.Provider}" />
        </StackPanel>
        <Label Grid.Row="3" FontSize="64" FontWeight="Bold" Content="{Binding Prize.Content}" />
        <StackPanel Grid.Row="4" Orientation="Horizontal">
            <Label FontSize="48" FontWeight="Bold" Content="{Binding Employee.SerialNumber}" Margin="0,0,20,0" />
            <Label FontSize="48" FontWeight="Bold" Content="{Binding Employee.Office}" Margin="0,0,20,0" />
            <Label FontSize="48" FontWeight="Bold" Content="{Binding Employee.Division}" Margin="0,0,20,0" />
            <Label FontSize="48" FontWeight="Bold" Content="{Binding Employee.Name}" Margin="0,0,20,0" />
            <Button Content="儲存" HorizontalAlignment="Left" VerticalAlignment="Center" Command="{Binding SaveWinnerCommand}" Margin="0,0,20,0" />
            <Label Content="{Binding SaveWinnerMessage}" VerticalAlignment="Center" />
        </StackPanel>
        <ListView Grid.Row="5" ItemsSource="{Binding Prize.Winners}">
            <ListView.View>
                <GridView AllowsColumnReorder="true">
                    <GridViewColumn DisplayMemberBinding="{Binding Path=SerialNumber}" Header="序號" Width="100" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Office}" Header="一級單位" Width="100" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Division}" Header="二級單位" Width="100" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=EmployeeID}" Header="員工代號" Width="100" />
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Header="姓名" Width="200" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
